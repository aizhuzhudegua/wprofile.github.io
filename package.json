{
  "name": "intro",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "git-commit-code": "git add . && git commit -m 'update'",
    "git-push-code": "git push origin main && git checkout main",
    "deploy": "git checkout release && npm run build  && git add -f dist && git commit -m 'update' && git subtree push --prefix dist origin release && git checkout main",
    "upload": "npm run git-commit-code && npm run git-push-code",
    "preview": "vite preview"
  },
  "dependencies": {
    "@element-plus/icons-vue": "^2.3.1",
    "element-plus": "^2.8.6",
    "three": "^0.169.0",
    "vue": "^3.5.12",
    "vuex": "next"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.1.4",
    "vite": "^5.4.9"
  }
}